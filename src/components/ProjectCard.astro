---
const { title, description, link } = Astro.props;
---

<div class="bg-white dark:bg-gray-800 shadow-md rounded-2xl p-6 hover:shadow-lg transition-shadow duration-300">
  <h3 class="text-xl font-semibold text-neutral-50 mb-2">{title}</h3>
  <p class="text-gray-600 dark:text-gray-300 mb-4">{description}</p>
  {link && (
    <a 
      href={link} 
      class="inline-block text-cyan-500 hover:text-cyan-600 font-medium transition-colors"
      target="_blank" rel="noopener noreferrer"
    >
      View Project →
    </a>
  )}
</div>
