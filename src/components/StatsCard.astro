---
    import '../styles/global.css';

    interface Props {
        title: string;
        value: number | string;
    }

    const { title, value, } = Astro.props;
---
<div>
  <p
    class="text-4xl font-bold text-cyan-800 dark:text-cyan-200 mb-2 text-center"
    data-target={value === '∞' ? 'infinity' : value}
  >
    0
  </p>
  <h2 class="text-1xl font-bold mb-6 text-center">{title}</h2>
</div>
