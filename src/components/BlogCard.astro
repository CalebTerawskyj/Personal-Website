---
const { post } = Astro.props;
---
<a href={`/blog/${post.slug}`} class="bg-white dark:bg-neutral-800 rounded-lg shadow-md overflow-hidden hover:shadow-lg transition-shadow duration-300">
    <div class="p-6">
        <h3 class="text-xl font-bold mb-2">{post.data.title}</h3>
        <p class="text-gray-700 dark:text-neutral-300 mb-4">{post.data.description}</p>
        <p class="text-gray-500 dark:text-neutral-400 text-sm">{post.data.date}</p>
    </div>
</a>